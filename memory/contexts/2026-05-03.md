# 2026-05-03 セッションブリーフィング

## サマリー
EmberChat マイク認識問題（entitlements不足）→ Privacy Settings アイコン問題（ヘルパーアプリ設定不足）を連続デバッグ。サーバー自動起動も整備。スキル2本新規作成。

## 引き継ぎ
- 継続テーマ: ad-hoc + Hardened Runtime + entitlements 構成は electron-builder で動く。Privacy Settings UI のアイコンはヘルパーアプリ経由で識別される
- 次のアクション:
  1. `feat-dashboard-static-serve` ブランチの merge 判断
  2. afterPack 設定 + entitlements.mac.plist + patch-mac-bundle.js を git commit
  3. EmberChat 次回起動でマイク自動許可されるか確認
- 注意事項: TCC.db は CDHash ベース。ad-hoc 署名で動くが将来仕様変更リスクあり

## 主要決定
- entitlements.mac.plist 作成 + package.json `build.mac` に hardenedRuntime + entitlements + entitlementsInherit 指定（マイク権限ダイアログが出る）
- afterPack `scripts/patch-mac-bundle.js` でメイン+全ヘルパーに icon.icns + CFBundleIconFile + CFBundleName 設定（Privacy Settings アイコン正常表示）
- Ember Chat.app をログイン項目に追加 + 全サーバー launchd 管理（PC再起動で完全自動復旧）

## 新規スキル
- `~/.claude/skills/ember-chat-mic-debug/SKILL.md` — マイク認識デバッグ（entitlements 起因含む）
- `~/.claude/skills/electron-mac-icon-debug/SKILL.md` — アイコン反映デバッグ（ヘルパーアプリ経由）
