# 2026-04-26 セッションブリーフィング

## サマリー
EmberChat 通常チャットパスにローカルLLM refusal → Claude (Slack Bot 経由) フォールバック実装。既存の always-on 資産 (`_wait_cache` + `_ask_slack_bot` + cooldown) を流用統合。同時に `_ask_slack_bot` の default system_prompt に Web ツール自発使用の誘導を追加し、天気・最新情報も実データ取得可能に（実機 19.6s で東京天気回答）。前半は会議録音実装と ambient 7壁突破、混合声紋プロファイル化。

## 引き継ぎ
- 継続テーマ: チャット Claude フォールバックの実運用観察 / 新 default system_prompt が always-on にも適用されるので過剰 WebSearch が起きないか様子見 / 混合声紋 sim 安定性 / 実マイク環境での会議録音精度
- 次のアクション:
  1. EmberChat から「今日の天気」「最新ニュース」等 end-to-end TTS で UX 確認
  2. `_TOOL_NEEDED_KEYWORDS` 事前ルーティングをチャットにも入れる（refusal 待ちなしで直接 Claude）— 必要性出たら v2
  3. 実会議 1件で録音 → 候補チップ承認 → 辞書蓄積運用ループ確認
- 注意事項: `_ask_slack_bot` default 変更は always-on にも影響、不具合時は呼び出し側で `system_prompt=""` バイパス可 / co_view `_BOT_REFUSAL_PATTERNS` と chat 側は別物維持 / ambient デバッグは `memory/voice_chat_ambient_layers.md` を最初に読む

## 主要決定
- EmberChat チャットパスに refusal 検知ブロック追加 (`app.py:6150-6160`) + `_CHAT_REFUSAL_PATTERNS` 定数化 (`app.py:4622`)
- `_ask_slack_bot` の default system_prompt で Web ツール自発使用を Claude に明示誘導
- co_view 側 `_BOT_REFUSAL_PATTERNS` との統合は見送り（要件異なる）
