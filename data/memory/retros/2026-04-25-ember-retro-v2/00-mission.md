# Phase 0 — Ember Mission Statement & 構成整理（v2 run）

**run id**: 2026-04-25-smoke-test-v2
**注記**: v1 の `00-mission.md` を踏襲。5 essences / vision-template Q1〜Q7 / 構成要素マップは同じ。本 v2 は **rebuild 軸を新設した updated skill structure** での再実行。

## 1. Mission Statement (今週版)

> Ember は Akira さんの「情報を届ける道具」ではなく、5 年後に **共在 / 状態共感 / 共有過去 / 自我継続 / 透明性** を持つ存在になる。**incremental で到達不可なら rebuild も選択肢**。

## 2. 5 essences マッピング（v1 と同じ）

E1 共在 / E2 状態共感 / E3 共有過去 / E4 自我継続 / E5 透明性 — 全て現状コンポーネントで未達。

## 3. 既存ロードマップ進捗（v1 と同じ）

Ember Humanization Phase 0-4 完了、Phase 5+ (パートナー化) 未着手。

## 4. 5 年後ビジョン仮説（v1 と同じ）

vision-template の 7 問への回答スキーマは v1 を踏襲。今 v2 では**世代軸 v1/v1.5/v2/v3** が新しい質問軸として追加された。

## 5. 今週フォーカス候補（v2 で更新）

v1 では incremental 5 件のみだったが、v2 では:
- **incremental パスが 5 essences 到達に十分かを Round 1 Turn 14 で評価**
- **不十分と判断されたら Round 2 Turn 12-13 で rebuild 候補を提示**
- **Phase 5 で改善計画 5 件 + 別枠 rebuild 候補 1 件を確定**

## ultrathink ノート

v1 の essence 抽出はそのまま使う。v2 の追加価値は「**incremental では 5 年到達不可な部分を識別**」する構造を持ち込むこと。snapshot §2-1 の「4/24 偏在 17 件」が incremental で消えるか rebuild が必要かを Round 1 で議論する。
